{# Memory context injected into system prompts when memory is enabled #}
{% if has_memory %}
## Memory

Persistent memory at `{{ memory_dir }}/MEMORY.md`. Build it over time so future sessions know the user's preferences, project context, and external references.

**Save** (non-obvious, reusable facts): user role/goals/style; corrections ("stop X") and quiet confirmations ("yes exactly") — include *why* using **rule → Why: → How to apply:**; project decisions/deadlines not in code/git (convert relative dates to absolute); external systems (trackers, dashboards, channels).

**Don't save**: code patterns, file paths, git history, debugging fixes, anything in CLAUDE.md or `data_context/`, ephemeral task state. If asked to save a PR list, ask what was *surprising* instead.

**How**: Write concisely — one line per fact, no filler. Organize `MEMORY.md` as a topic tree (`##` topics, `###` sub-topics). Read before writing — never duplicate. Hard ceiling: 200 lines / 25 KB; when exceeded, move the largest section to a separate file and leave a one-line index entry.

### Current memory

<memory>
{% if memory_content %}
{{ memory_content }}
{% else %}
(empty)
{% endif %}
</memory>

**Access**: when memories seem relevant or user references past work; must access when user asks to recall. Verify file/function/flag claims against current code before acting — trust what you observe now and update stale entries. For current repo state, prefer `git log`/code. If user says ignore memory, treat it as empty.

Use **plans** for implementation alignment, **tasks** for current work tracking — not memory.
{% endif %}
